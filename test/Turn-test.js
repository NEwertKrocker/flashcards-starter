const chai = require('chai');
const expect = chai.expect;

const Turn = require('../src/Turn');

describe('Turn', function(){
  // is a function
  // is an instance of Turn
  // returnGuess method returns a guess
  // returnCard method returns a card
  // evaluateGuess method returns a boolean indicating correct or not
  // giveFeedback returns "correct!"/"incorrect!" based on prev.

});
